extends layout

block content
	h1 #{title}

		p Do you really want to delete this Category?

		form(method='POST' action='')
			div.form-group
				input#categoryid.form-control(type='hidden' name='categoryid' required='true', value=category_id )

			button.btn.btn-primary(type='submit') Delete