extends layout

block content
	h1 #{title}

		p Do you really want to delete this Item?

		form(method='POST' action='')
			div.form-group
				input#itemid.form-control(type='hidden' name='itemid' required='true', value=item_id )

			button.btn.btn-primary(type='submit') Delete